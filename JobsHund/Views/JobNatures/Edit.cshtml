@model jobhundClassLibrary1.JobNature

@{
    ViewBag.Title = "Edit";
}



<div class="section-top-border">
    <div class="row">
        <div class="col-lg-5 col-md-5">
            <h3 class="mb-30">Edit Job Nature   </h3>
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                @Html.HiddenFor(model => model.JobNatureId)

                <div class="mt-4">

                    @Html.EditorFor(model => model.JobNature1, new
               {
                   htmlAttributes = new
                   {
                       @class = "form-control single-input",
                       @name = "first_name",
                       @placeholder = "Enter Job Nature",
                       @onfocus = "this.placeholder = ''",
                       @onblur = "this.placeholder = 'Enter Job Nature'",
                       @required = "true"
                   }
               })
                    @Html.ValidationMessageFor(model => model.JobNature1, "", new { @class = "text-danger" })

                </div>

                <div class="form-group mt-4">

                    <input type="submit" value="Update" class="genric-btn success" style="float:right;" />
                    @Html.ActionLink("Back to List", "Index", null, new { @Class = "genric-btn danger", @Style = "float:left;" })

                </div>

            }

            <div>

            </div>
        </div>
    </div>
</div>
